{
  "name": "@repo/data-validation",
  "version": "0.0.1",
  "scripts": {
    "build": "rm -rf dist && tsc",
    "lint": "eslint 'src/**/*.ts'",
    "lint:fix": "eslint 'src/**/*.ts' --fix",
    "dev": "tsc --watch --preserveWatchOutput --pretty"
  },
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./dist/index.js"
    }
  },
  "dependencies": {
    "ioredis": "^5.4.1",
    "redis": "^4.7.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@repo/db": "workspace:*",
    "@servly/typescript-config": "workspace:*",
    "@servly/eslint-config": "workspace:*"
  }
}
